import { GetAllSharedRequest } from './get-all-shared-request';

describe('GetAllSharedRequest', () => {
  it('should create an instance', () => {
    expect(new GetAllSharedRequest()).toBeTruthy();
  });
});
